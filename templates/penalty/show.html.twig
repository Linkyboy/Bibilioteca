{% extends 'base.html.twig' %}

{% block title %}Penalty{% endblock %}

{% block body %}
    <h1>Penalty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ penalty.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ penalty.date ? penalty.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Duration</th>
                <td>{{ penalty.duration ? penalty.duration|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ penalty.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('penalty_index') }}">back to list</a>

    <a href="{{ path('penalty_edit', {'id': penalty.id}) }}">edit</a>

    {{ include('penalty/_delete_form.html.twig') }}
{% endblock %}
