{% extends 'base.html.twig' %}

{% block title %}Copy index{% endblock %}

{% block body %}
    <h1>Copy index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>CopyReference</th>
                <th>Status</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for copy in copies %}
            <tr>
                <td>{{ copy.id }}</td>
                <td>{{ copy.copyReference }}</td>
                <td>{{ copy.status }}</td>
                <td>
                    <a href="{{ path('copy_show', {'id': copy.id}) }}">show</a>
                    <a href="{{ path('copy_edit', {'id': copy.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('copy_new') }}">Create new</a>
{% endblock %}
