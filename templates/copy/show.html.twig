{% extends 'base.html.twig' %}

{% block title %}Copy{% endblock %}

{% block body %}
    <h1>Copy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ copy.id }}</td>
            </tr>
            <tr>
                <th>CopyReference</th>
                <td>{{ copy.copyReference }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ copy.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('copy_index') }}">back to list</a>

    <a href="{{ path('copy_edit', {'id': copy.id}) }}">edit</a>

    {{ include('copy/_delete_form.html.twig') }}
{% endblock %}
